% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_dispatch.R
\name{extract_schema}
\alias{extract_schema}
\title{Detect ontology style and dispatch to the matching extractor}
\usage{
extract_schema(source)
}
\arguments{
\item{source}{A SPARQL endpoint URL or a parsed \code{rdflib} compatible file.}
}
\value{
A schema list, see \code{\link[=extract_schema_owl]{extract_schema_owl()}}.
}
\description{
Probes \code{source} for an OWL signal, then a VoID signal, and dispatches to
\code{\link[=extract_schema_owl]{extract_schema_owl()}}, \code{\link[=extract_schema_void]{extract_schema_void()}} or
\code{\link[=extract_schema_generic]{extract_schema_generic()}} accordingly. The VoID probe is a single
\code{UNION} query (rather than two separate ones) to keep detection to at
most 2 HTTP round trips before real extraction starts.
}
\details{
The three styles are treated as mutually exclusive: whichever signal is
found first wins, and the others are never inspected. This is a good fit
for a single ontology file, but a dataset that mixes styles (e.g. a VoID
description published alongside an OWL vocabulary) will only ever be
read through one lens, so classes/properties that only appear as plain
\code{rdf:type}/predicate usage in an otherwise-OWL graph are not merged in.
}
