% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_visualize.R
\name{visualise_schema}
\alias{visualise_schema}
\title{Render a schema graph as an interactive visNetwork widget}
\usage{
visualise_schema(
  g,
  title = "Schema Graph",
  max_nodes = 750,
  physics_threshold = 150,
  force = FALSE
)
}
\arguments{
\item{g}{An \code{igraph} graph, see \code{\link[=build_schema_graph]{build_schema_graph()}}.}

\item{title}{Character scalar shown as the widget's title.}

\item{max_nodes}{Refuse to render above this many nodes unless
\code{force = TRUE}. Default \code{750}.}

\item{physics_threshold}{Above this many nodes, use a precomputed
static layout and disable the running physics simulation. Default
\code{150}.}

\item{force}{If \code{TRUE}, render anyway above \code{max_nodes} (with a
warning). Default \code{FALSE}.}
}
\value{
A \code{visNetwork} htmlwidget.
}
\description{
Refuses to render graphs above \code{max_nodes} by default (an interactive
force-directed layout with thousands of nodes is unreadable and can
freeze a browser tab). Graphs above \code{physics_threshold} get a
precomputed static layout and straight edges instead of a running
physics simulation, which is what actually makes large graphs feel
sluggish in-browser.
}
